namespace WpfSimpleCalculator.Models
{
    public enum eOperator   
    {
        Plus = (int)'+',
        Minus = (int)'-',
        Multiply = (int)'x',
        Divide = (int)'÷',
        Clear = (int)'C',
        Equals = (int)'='
    }
}